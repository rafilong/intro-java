import java.awt.Color;


public class Square {
	int squareid;
	int tetrimoid;
	Color color = Color.WHITE;
	int xloca;
	int yloca;
	
	boolean leftn = false;
	boolean rightn = false;
	boolean topn = false;
	boolean bottomn = false;
	
	int nextid = 0;
	
	public Square(int sid, int tid, int x, int y) {
		squareid = sid;
		tetrimoid = tid;
		xloca = x;
		yloca = y;
	}
	
	public static void neighborfind(Square[][] grid) {
		for (int x = 0; x < Main.gamewidth; x++) {
			for (int y = 0; y < Main.gameheight; y++) {
				grid[x][y].leftn = sametetrimoid(grid[x][y], grid[x-1][y]);
				grid[x][y].rightn = sametetrimoid(grid[x][y], grid[x+1][y]);
				grid[x][y].topn = sametetrimoid(grid[x][y], grid[x][y+1]);
			}
		}
		
		sametetrimoid(grid[1][1], grid[2][2]);
	}
	
	public static boolean sametetrimoid(Square s1, Square s2) {
		return s1.tetrimoid == s2.tetrimoid;
	}
}